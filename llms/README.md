# LLM References

This folder contains large local reference files for AI agents.

## Effect

`effect-ts-llm.txt` was downloaded from:

```text
https://raw.githubusercontent.com/legions-developer/invoicely/refs/heads/main/llm/effect-ts-llm.txt
```

It is intentionally large. Agents should not load the whole file into context by default. Use targeted reads only when implementing Effect-specific code or architecture.
